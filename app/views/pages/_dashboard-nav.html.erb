<ul class="nav nav-tabs" id="myTab" role="tablist">
  <li class="nav-item" role="presentation">
    <button class="nav-link active" id="booked-tab" data-bs-toggle="tab" data-bs-target="#booked" type="button" role="tab" aria-controls="booked" aria-selected="true">Booked</button>
  </li>
  <li class="nav-item" role="presentation">
    <button class="nav-link" id="arts-to-book-tab" data-bs-toggle="tab" data-bs-target="#arts-to-book" type="button" role="tab" aria-controls="arts-to-book" aria-selected="false">My arts to book</button>
  </li>
  <li class="nav-item" role="presentation">
    <button class="nav-link" id="pending-bookings-tab" data-bs-toggle="tab" data-bs-target="#pending-bookings" type="button" role="tab" aria-controls="pending-bookings" aria-selected="false">Pending bookings</button>
  </li>
</ul>
<div class="tab-content" id="myTabContent">
  <div class="tab-pane fade show active" id="booked" role="tabpanel" aria-labelledby="booked-tab"><%= render "pages/card-index-booking" %></div>
  <div class="tab-pane fade" id="arts-to-book" role="tabpanel" aria-labelledby="arts-to-book-tab" class="tab-pane fade" id="arts-to-book" role="tabpanel" aria-labelledby="arts-to-book-tab">

<% @arts.each do |art| %>
  <% if art.user == current_user %>
  <div class="container-md">
      <div class="card-product">
        <div class="card-product-infos">
          <h3 class="card-title">Art piece: <%= art.title %></h3>
          <p><strong>Booking price per day:</strong> <%= number_to_currency(art.price) %></p>
        </div>
      </div>
    </div>
  <% end %>
<% end %>

</div>
</div>
  <div class="tab-pane fade" id="pending-bookings" role="tabpanel" aria-labelledby="pending-bookings-tab">...</div>
</div>
